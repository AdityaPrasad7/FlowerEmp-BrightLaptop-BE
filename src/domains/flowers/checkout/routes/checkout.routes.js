/**
 * Checkout Routes (Flowers Domain)
 * Note: Checkout is handled through cart routes
 * This file is kept for future extensibility
 */
import express from 'express';

const router = express.Router();

// Checkout is handled via POST /api/flowers/cart/checkout
// See cart.routes.js for implementation

export default router;


